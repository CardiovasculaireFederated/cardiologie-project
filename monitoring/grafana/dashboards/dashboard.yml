apiVersion: 1

providers:
  - name: cardio-dashboards
    orgId: 1
    folder: Cardio
    type: file
    overwrite: true
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/json
